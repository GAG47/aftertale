# Phase 67 Region Location Graph Completion

This file is retained only as a superseded record.

The Region Location Graph completion path was withdrawn after v67.3 was
re-centered on Location nodes as the only current compiler product.

The active boundary is now:

```text
RegionInput
-> SemanticRoleResult
-> LocationNodeResult
-> v67.4 boundary
```

The current implementation does not generate edge contracts, graph snapshots,
runtime graph registration, graph travel, scene exits, or generated Location
Scenes.

The current v67.3 record is:

```text
doc/phase_67_3_location_nodes.md
```
